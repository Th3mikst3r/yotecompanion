<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:paddingLeft="0dp"
    android:paddingRight="0dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:background="?attr/selectableItemBackground"
    android:focusable="true">

    <de.hdodenhof.circleimageview.CircleImageView
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:src="@mipmap/ic_launcher"
        android:id="@+id/img_note_icon"/>


    <TextView
        android:id="@+id/lbl_habitName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="16dp"
        android:layout_alignParentTop="true"
        android:textColor="@color/colorSecondaryText"
        android:layout_marginTop="5dp"
        android:layout_toRightOf="@+id/img_note_icon"
        android:layout_marginLeft="10dp"
        android:maxLength="20"
        android:ellipsize="end"
        android:maxLines="1" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/lbl_habitName"
        android:id="@+id/lbl_numOfTimes"
        android:textColor="@color/colorSecondaryText"
        android:layout_marginTop="10dp"
        android:layout_toRightOf="@+id/img_note_icon"
        android:layout_marginLeft="10dp"
        android:maxLength="30"
        android:ellipsize="end"
        android:maxLines="1" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/lbl_numOfTimes"
        android:id="@+id/lbl_date"
        android:textColor="@color/colorSecondaryText"
        android:layout_marginTop="10dp"
        android:layout_toRightOf="@+id/img_note_icon"
        android:layout_marginLeft="10dp"
        android:maxLength="30"
        android:ellipsize="end"
        android:maxLines="1" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/lbl_date"
        android:id="@+id/lbl_time"
        android:textColor="@color/colorSecondaryText"
        android:layout_marginTop="10dp"
        android:layout_toRightOf="@+id/img_note_icon"
        android:layout_marginLeft="10dp"
        android:maxLength="30"
        android:ellipsize="end"
        android:maxLines="1" />


</RelativeLayout>
